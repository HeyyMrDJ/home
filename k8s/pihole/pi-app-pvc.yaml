apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pi-app-pvc
spec:
  volumeName: pi-app-pv
  storageClassName: ""
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500M
